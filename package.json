{
  "name": "better-hono-react-monorepo",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "deploy": "turbo deploy",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "dev:docker": "cd apps/api && docker-compose up -d",
    "dev:docker:down": "cd apps/api && docker-compose down",
    "generate:types": "turbo generate:types"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.3"
  },
  "packageManager": "pnpm@9.15.2",
  "engines": {
    "node": ">=18"
  }
}
